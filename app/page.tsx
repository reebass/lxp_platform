// Здесь нет "use client", так как это серверный компонент.
// По умолчанию все компоненты в Next.js App Router (папка app) рендерятся на сервере (Server Components).
// Это улучшает SEO и ускоряет начальную загрузку страницы, так как браузер получает сразу готовый HTML-код, 
// а не ждет загрузки JavaScript, чтобы отрисовать страницу.

// Импортируем компонент LoginForm, который мы вынесли в отдельный файл.
// Выносим форму в отдельный компонент для переиспользования и чистоты кода.
// Использование компонентного подхода делает код модульным, легко читаемым и масштабируемым.
// В серверный компонент page.tsx безопасно импортировать клиентские компоненты (как наш LoginForm).
import LoginForm from "@/components/auth/LoginForm";

// Это главная функция страницы авторизации, которая собирает всё воедино.
export default function LoginPage() {
  return (
    // Главный контейнер (обертка) для всей страницы. Выставляет фон и центрирует элементы по центру экрана.
    <div className="min-h-screen bg-neutral flex flex-col items-center justify-center p-4">
      {/* Выводим (рендерим) саму форму логина, которую мы импортировали выше. */}
      {/* Теперь страница остается чистой, а вся сложная логика скрыта внутри компонента LoginForm */}
      <LoginForm />
    </div>
  );
}
